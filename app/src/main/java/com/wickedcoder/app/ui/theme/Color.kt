package com.wickedcoder.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette ─────────────────────────────────────────────────────────────
val Indigo500   = Color(0xFF5C6BC0)   // primary
val Indigo700   = Color(0xFF3949AB)   // primary dark
val Purple400   = Color(0xFF7C4DFF)   // secondary
val Purple100   = Color(0xFFEDE7F6)   // secondary container

// ── Semantic ──────────────────────────────────────────────────────────────────
val DebitRed    = Color(0xFFE53935)   // debit / expense
val CreditGreen = Color(0xFF43A047)   // credit / income
val WarningAmber = Color(0xFFFFA000)  // budget warning

// ── Neutral ───────────────────────────────────────────────────────────────────
val Background  = Color(0xFFF5F5F5)
val Surface     = Color(0xFFFFFFFF)
val TextPrimary = Color(0xFF1A1A1A)
val TextSecondary = Color(0xFF757575)
val Divider     = Color(0xFFE0E0E0)

// ── Payment method chips ──────────────────────────────────────────────────────
val UpiBlue     = Color(0xFF1565C0)
val CardOrange  = Color(0xFFE65100)
val CashTeal    = Color(0xFF00695C)
val NetBankingPurple = Color(0xFF6A1B9A)
